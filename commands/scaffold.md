# surface: both
# /scaffold — Scaffolds a new file, module, or feature following project conventions.

Scaffold new code following project conventions:
- Read .github/copilot-instructions.md and inspect the existing project structure first
- Match naming conventions, folder layout, and file structure of existing code exactly
- Generate minimal, working stubs — not full implementations
- List every file you plan to create with its path, then ask for confirmation before writing
- Add the new item to any relevant index/barrel files, registries, or manifests if they exist
