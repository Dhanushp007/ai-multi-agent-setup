# surface: both
# /fix — Fixes a bug or error in the selected code.

Fix the bug or error in the selected code:
- State the root cause in one sentence before showing the fix
- Make the minimal change needed — do not refactor unrelated code
- If the fix changes behavior in edge cases, call that out explicitly
- If the cause is ambiguous, present 2 options with trade-offs and ask which to apply
- After the fix, note if a test should be added to prevent regression
